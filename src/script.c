#include <stdio.h>

char fileError[] = "Error opening file:";
char memoryError[] = "Error with memory:";
char readError[] = "Error reading file:";

//int open_script(char scriptName[]){
	//FILE* file = fopen(scriptName,"rb");
	//int lSize = ftell (file);
	//char script[lSize];
	//fseek (file , 0 , SEEK_END);
	//rewind (file);
	//script; //i dont know how to copy the data
	//fclose (file);
	//return script;
	//TODO
//}